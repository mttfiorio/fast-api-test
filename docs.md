# Notes

Running pip will install packages system wise. A better idea is creating an environment: `python -m venv ENV_NAME`
From inside the environment we can then install packages `pip install fastapi uvicorn`

Fast API comes with Swagger at /docs: `http://localhost:8000/docs`
